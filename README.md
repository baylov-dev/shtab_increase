# 🤖 ИНКРИСШТАББОТ

Телеграм-бот для администрирования супергрупп с темами (Topics).

## Стек
- Python 3.11
- aiogram 3.x
- aiosqlite (WAL mode)
- apscheduler
- Docker & Docker Compose

## Функционал
- **Админ-панель (`/admin`)**: Полное управление через Inline-кнопки.
- **Напоминания**: Создание задач для конкретных топиков с выбором дней недели.
- **Оптимизация**: Стратегия "Zero DB Hits" для отслеживания топиков (кэширование в RAM).
- **Настройки**: Включение/выключение приветствий, выбор топика для приветствий.
- **Безопасность**: Автоудаление системных сообщений о вступлении в группу.
- **Бэкап**: Возможность моментально скачать файл базы данных.

## Установка (Docker)

1. Клонируйте репозиторий.
2. Подготовьте файл `.env` (используйте `.env.example` как образец):
   ```bash
   cp .env.example .env
   ```
3. Отредактируйте `.env`, вставив ваш `BOT_TOKEN` и `ADMIN_ID`.
3. Запустите:
   ```bash
   docker-compose up -d
   ```

## Установка (Локально)

1. Установите зависимости:
   ```bash
   pip install -r requirements.txt
   ```
2. Создайте `.env` и добавьте `BOT_TOKEN` и `ADMIN_ID`.
3. Запустите:
   ```bash
   python main.py
   ```

## Автор
Разработано Senior Python Developer / DevOps.
